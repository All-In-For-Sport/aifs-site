---
interface Props {
  class?: string;
}
const { class: className } = Astro.props;
---

<span class:list={['rounded-full bg-gray-800 px-4 py-2 text-xs font-bold uppercase tracking-wider', className]}>
  <slot />
</span>
